# Automated test report

Date: 2026-08-01
OS: Linux 7.0.0-28-generic x86_64  
Node: 22.16.0  
pnpm: 10.32.1

## Passing gates

| Gate                                           | Result                                                             |
| ---------------------------------------------- | ------------------------------------------------------------------ |
| Isolated clean Git snapshot and frozen install | Pass; status clean before and after all gates                      |
| Formatting, ESLint and strict Vue/TypeScript   | Pass                                                               |
| Unit tests                                     | 214 pass in 37 files                                               |
| Resolver/PAC parity and fuzz                   | 5 pass                                                             |
| Coverage suite                                 | 221 pass in 39 files                                               |
| Coverage                                       | 89.16% statements, 79.77% branches, 93.16% functions, 89.46% lines |
| Idempotent GitHub Release publisher            | 6 local create/retry/failure/checksum regressions pass             |
| Release performance budgets                    | 5 pass across unit and browser E2E                                 |
| Five-minute Chromium memory soak               | 752 cycles; heap +0.96 MiB, PSS +55.15 MiB; all budgets pass       |
| Local network and Firefox API integration      | 6 pass in 2 files                                                  |
| Chromium extension E2E                         | 40 pass                                                            |
| Headed Orca/AT-SPI accessibility               | Keyboard speech output and actual 200% zoom pass                   |
| Docs, spelling, workflow and traceability      | 126 tasks and 150 requirements covered                             |
| Store asset dimensions                         | 300×300, 440×280 and 1280×800 PNG candidates pass                  |
| Dependency audit                               | No known vulnerabilities                                           |
| Production builds                              | Chromium MV3 and Firefox MV3 pass                                  |
| Manifest, CSP, icon and permission validation  | Pass                                                               |
| Permission-warning extraction                  | Chromium 151 and Firefox 153 reviewed                              |
| Artifact canary/dynamic-code scan              | Pass                                                               |
| ZIP content validation                         | Pass                                                               |
| Firefox source-package exact rebuild           | 20 release files reproduced with matching SHA-256                  |
| SHA-256 verification                           | Pass                                                               |

The E2E suite covers the visible options, popup and error surfaces; the standalone Settings-tab
contract; profile/rule operations; explicit per-rule profile assignment; `.ru`/`.рф`/`.de` suffix
routes through two local proxies; empty first-run rules without group management; Once and Always
popup actions; applied routing; fail-closed behavior; proxy authentication; distinct HTTP/HTTPS
endpoints; HTTP and HTTPS proxy transports; WS/WSS; downloads; manual proxy check and cancellation;
Firefox browser-manual fallback, second-extension precedence/recovery and locked enterprise policy;
import/export; themes; accessibility; drag/keyboard ordering; native import replacement; local logs;
incognito help; another real Chromium proxy extension; and cold/warm performance thresholds.

All integration targets and proxies run on loopback. The test suite does not use a public proxy or
random external service.

The final current tree was copied into an isolated Git repository with no dependencies or
generated output. From an empty Git status it passed frozen installation, strict quality, 214 unit
tests, 5 parity tests, 221 coverage tests, performance, 6 integration tests, all 40 E2E tests,
dependency audit, dual builds, permission-warning/canary gates, all three ZIP packages, exact
Firefox source rebuild and deep archive scanning. Git status was still empty afterward; the
disposable clone was then moved to trash.

That run exposed a test-harness cleanup issue rather than a product defect: terminating `web-ext`
could leave its autogenerated Firefox profile outside the spike directory. Twenty older profiles
containing the exact spike add-on ID were moved to trash; one unmarked profile was deliberately
left untouched. The current API harness installs temporary add-ons through WebDriver BiDi and
passes an exact per-test `--profile-root`; every source/profile directory is removed in `finally`.

A second isolated repository recorded commit
`c3062b369cbb36ab6c1e48c79ae5dfe86c5ab66e`, tagged it `v0.1.0`, verified the tag/version gate and
ran the dual build, manifest/warning/canary checks, ZIP packaging, package validation, exact
Firefox source rebuild and portable SHA-256 generation. The release publisher additionally has
local regressions for first creation, same-release asset refresh with `--clobber`, unsafe existing
release rejection and post-download SHA-256 verification. Git status remained empty before and
after; the temporary tagged repository was then moved to trash. This is local PL-111/PL-112
automation evidence, not a hosted GitHub Release claim.

## Browser evidence boundaries

- Playwright Chromium 151.0.7922.34: full automated suite.
- Firefox 153.0: real MV3 proxy API/integration gate selected by ADR-011 plus a headed,
  clean-profile full-product smoke in the official Snap Stable channel from
  `.output/firefox-mv3`. Manual fallback/null/array semantics, settings reload, real
  second-extension recovery, locked enterprise policy, Options/Profile/Popup state, regular and
  private request-time proxy routing, a 70-second event-page idle wake, private-memory log cleanup,
  storage redaction and DIRECT restoration passed; see `firefox-153-linux.md`.
- Google Chrome 150.0.7871.128 Official Stable: command-line extension flags were not accepted, but
  clean-profile GUI sideload, UI/proxy-control smoke and a real incognito-window lifecycle passed;
  see `chrome-150-stable-linux-smoke.md`.
- Microsoft Edge 150.0.4078.99 Stable: the official Microsoft Linux package was extracted without
  system installation. Clean-profile GUI sideload, UI/proxy-control, regular and real InPrivate
  routing, private-memory logging, storage redaction and DIRECT restoration passed; see
  `edge-150-stable-linux-smoke.md`.
- Yandex Browser 26.4.1.1110 stable (Chromium 146.0.7680.1110): command-line extension flags did
  not register the MV3 worker, but clean-profile GUI sideload and core UI/proxy-control smoke
  passed; see `yandex-26-stable-linux-smoke.md`.

This report is automated candidate evidence supplemented by recorded Chrome, Firefox, Edge and
Yandex runtime smokes, an isolated real-Orca walkthrough and a five-minute Chromium memory soak.
It does not sign the full package-install/update or store-portal manual matrices.
